
server:
  servlet:
    context-path: /order-service

spring:
  config:
    import: optional:configserver:http://localhost:9296
  application:
    name: ORDER-SERVICE
  profiles:
    active:
    - dev
  jackson:
    serialization:
      indent-output: true

