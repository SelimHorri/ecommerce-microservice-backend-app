
server:
  servlet:
    context-path: /shipping-service

spring:
  config:
    import: optional:configserver:http://localhost:9296
  application:
    name: SHIPPING-SERVICE
  profiles:
    active:
    - dev
  jackson:
    serialization:
      indent-output: true

