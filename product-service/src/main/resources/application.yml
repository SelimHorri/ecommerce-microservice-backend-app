
server:
  servlet:
    context-path: /product-service

spring:
  config:
    import: optional:configserver:http://localhost:9296
  application:
    name: PRODUCT-SERVICE
  profiles:
    active:
    - dev
  jackson:
    serialization:
      indent-output: true

