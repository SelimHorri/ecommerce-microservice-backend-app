
server:
  servlet:
    context-path: /payment-service

spring:
  config:
    import: optional:configserver:http://localhost:9296
  application:
    name: PAYMENT-SERVICE
  profiles:
    active:
    - dev
  jackson:
    serialization:
      indent-output: true

