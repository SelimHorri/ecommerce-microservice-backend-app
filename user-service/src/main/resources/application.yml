
server:
  servlet:
    context-path: /user-service

spring:
  config:
    import: optional:configserver:http://localhost:9296
  application:
    name: USER-SERVICE
  profiles:
    active:
    - dev
  jackson:
    serialization:
      indent-output: true

